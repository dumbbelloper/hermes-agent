# 자율 운영 merge 후 production smoke 검증

- 작업 일시: 2026-08-11T03:48:54Z ~ 2026-08-11T03:57:07Z
- 상태: 검증 완료, 운영 활성
- task ID: `post-merge-20260811T035700Z`

## 사용자 요청과 목적

사용자가 [PR #14](https://github.com/dumbbelloper/hermes-agent/pull/14)를 merge한 뒤 자율 결제 뉴스 운영을 계속 진행하도록 요청했다. 목적은 GitHub merge와 CI를 원문에서 확인하고, root checkout을 clean `main` coordinator로 복원한 다음 switch, quota, repository gate, Gateway, 3시간 cron, source precheck와 unattended-run 정리 경로를 production 상태에서 검증하는 것이다.

## 수행한 변경

- GitHub에서 PR #14가 `MERGED`이고 merge commit이 `00e54d381baa1bcbbc5fcc357dcbc63eb34fe6c1`임을 확인했다.
- Ubuntu, macOS, Windows의 `Skill validation` check가 모두 성공했음을 확인했다.
- root checkout이 clean `main`인지, fetch URL과 모든 push URL이 `dumbbelloper/hermes-agent` allowlist와 일치하는지 확인했다.
- origin 전체 ref를 fetch하고 `git merge --ff-only origin/main`을 실행한 뒤 `HEAD == origin/main`을 검증했다.
- [Autonomy controller](../../../Automation/autonomy.py)의 status와 설치된 wrapper precheck를 실행했다.
- live Codex primary quota가 사용률 30%, 잔여율 70%로 허용 상태이며 repository gate가 ready임을 확인했다.
- precheck가 run `20260811T035327044611Z-62a296c1`에 queue item 1개를 생성하고 `wakeAgent: true`를 반환했다.
- 해당 item의 [American Express 공식 원문](https://www.americanexpress.com/en-us/newsroom/articles/entertainment-and-experiences/american-express-unveils-new-tennis-experiences-for-all-fans-and.html)을 추출해 US Open lounge·fan perk·event promotion임을 검증했다.
- 결제 인프라, 정산, 인증, 사기 방지, 규제 또는 직접 관련 fintech 변화가 아니므로 `irrelevant`로 기록했다.
- queue empty, Telegram delivery 0건, `automation-finish` 성공과 active-run lock 해제를 확인했다.
- 같은 cron job을 다시 직접 실행해 새 delta가 없을 때 `wakeAgent: false`로 agent를 깨우지 않고 정상 종료하는 것을 Gateway log에서 확인했다.

## 생성·수정한 문서와 파일

- 이 task log만 생성했다.
- root checkout의 추적 파일은 수정하지 않았다.
- shared local runtime state인 `.hermes-news/data`에는 source snapshot, run manifest, queue outcome과 decision ledger가 정상 갱신됐다.

## 실행한 검증과 결과

- `gh pr view 14`: `MERGED`, merge commit `00e54d3`, 3개 OS CI `SUCCESS`
- `git fetch origin && git merge --ff-only origin/main`: `Already up to date`
- `HEAD == origin/main`: 통과
- fetch URL 및 `git remote get-url --push --all origin` allowlist: 통과
- `Automation/autonomy.py status`: switch enabled, repository ready, quota allowed
- installed/tracked wrapper byte 비교: 일치
- installed wrapper mode `0700`, switch file mode `0600`: 통과
- Python bytecode compilation: 통과
- Automation unit tests: 87개 통과
- registry와 Vault note validation: 통과
- Gateway: launchd supervised, running
- cron job `b83df32a868f`: active, 180분 주기, direct execution completed
- unattended run `20260811T035327044611Z-62a296c1`: `completed`, outcome `irrelevant: 1`, source failure 0
- 후속 no-change cron: `wakeAgent: false`, `[SILENT]`, execution completed
- root `git status`: clean `main...origin/main`
- PR #15 CI 1차: Ubuntu 9초, macOS 14초, Windows 24초로 모두 통과

## 내린 결정과 근거

- source title에 카드가 언급되더라도 주된 내용이 스포츠 event promotion과 lounge·perk 소개이므로 문서화하지 않았다. automation artifact schema의 Curator rule이 event promotion을 명시적으로 제외한다.
- smoke 과정에서 wrapper를 직접 실행하면 precheck가 durable run과 lock을 실제 생성하므로, 반환된 run을 방치하지 않고 claim, deterministic reject, notify, finish 순서로 정상 종료했다.
- 관련성이 없는 item에 대해 빈 문서나 PR을 만들지 않았다. 불필요한 Vault 변경과 Telegram 중복을 방지하기 위해서다.
- root checkout은 계속 coordinator 전용 clean `main`으로 유지하고, 이 기록은 독립 worktree와 `automation/*` branch에서 작성한다.

## 전역 설정이나 외부 시스템에 적용한 변경

- Hermes Gateway, cron schedule, recipient, credential, quota threshold와 standing authorization 정책은 변경하지 않았다.
- GitHub PR #14 merge 상태와 CI를 읽기 전용으로 확인했다.
- cron job `b83df32a868f`를 direct smoke run으로 두 번 실행했다.
- relevant document가 없어 Telegram 문서 전송은 0건이었다.
- source decision과 delivery idempotency를 위한 `.hermes-news/data`의 local durable state가 갱신됐다.
- 이 task log를 `automation/news-post-merge-20260811T035700Z` branch에 commit·push하고 [PR #15](https://github.com/dumbbelloper/hermes-agent/pull/15)를 생성했다. 자동 merge는 수행하지 않았다.
- task log 전송의 첫 시도는 잘못된 global `--config` 위치 때문에 CLI argument validation에서 종료되어 외부 전송이 없었다. 올바른 subcommand option `--telegram-config`로 다시 실행해 고정 Telegram recipient에 문서 1개를 2개 message로 전송했고 `sent`를 확인했다.

## 알려진 한계와 남은 작업

- 이번 live delta는 event promotion으로 판정되어 독립 news worktree에서 Inbox 문서 작성, commit, push, PR 생성까지의 relevant-item 경로는 실행되지 않았다.
- 다음 relevant official-source delta가 발견되면 예정된 3시간 cron cycle에서 해당 경로를 자동 실행한다.
- Mac이 sleep이거나 로그인 session이 중단된 동안 강제로 깨우지 않으며, resume 후 다음 cycle에서 동작한다.
- 자동 merge는 계속 금지되며 생성된 news PR은 사용자가 검토·merge한다.
