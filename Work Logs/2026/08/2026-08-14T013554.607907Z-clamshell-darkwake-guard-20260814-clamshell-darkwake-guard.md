# macOS clamshell DarkWake 자동화 timeout 방지

- 작업 일시: 2026-08-14 01:26–01:36 UTC, 운영 반영 검증 2026-08-18 04:45–06:04 UTC
- task ID: `clamshell-darkwake-guard-20260814`
- 상태: main merge·운영 반영·fully-awake manual execution 검증 완료

## 사용자 요청과 목적

사용자는 자율 결제 뉴스 cron에 실패 이력이 반복되는 원인과 무인 자동화가 안정적으로 동작하지 않는 이유를 확인해 달라고 요청했다. 새 prompt 적용 후 발생한 timeout을 과거 실패와 분리해 조사하고, 닫힌 MacBook의 짧은 DarkWake에서 agent가 시작됐다가 다시 잠들어 timeout되는 반복을 방지하는 것이 목적이다.

## 수행한 변경

- cron execution DB와 agent log를 대조해 새 prompt 적용 후 4회 성공 뒤 2026-08-14에 3회 연속 timeout이 발생했음을 확인했다.
- macOS `pmset -g log`와 실행 시각을 대조해 세 실행 모두 44–60초짜리 DarkWake에서 시작된 뒤 932–965초간 clamshell sleep에 들어간 사실을 확인했다.
- 마지막 active run `20260814T004919906742Z-9416a2bc`의 Amex 고객 만족도 수상 자료를 결제 인프라 변경이 아닌 홍보 자료로 판정하고 controller 명령으로 `irrelevant` 종료했다.
- 반복 실패를 막기 위해 live cron job `b83df32a868f`를 일시 정지했다.
- `Automation/autonomy.py`에 macOS `AppleClamshellState` probe를 추가했다.
- enabled switch 확인 직후 닫힌 clamshell이면 quota 조회, Git 동기화와 source precheck 전에 `wakeAgent: false`, `reason: clamshell_closed`로 정상 종료하도록 했다.
- probe를 5초로 제한하고 non-macOS, command failure와 malformed output에서는 기존 동작을 유지했다.
- closed-lid 판정과 외부 gate 선행 skip을 고정하는 regression test 두 개를 TDD로 추가했다.
- macOS 운영 조건과 `clamshell_closed`의 처리 지연 의미를 운영 문서에 반영했다.

## 생성·수정한 문서와 파일

- [자율 운영 controller](../../../Automation/autonomy.py)
- [자율 운영 테스트](../../../Automation/tests/test_autonomy.py)
- [Automation README](../../../Automation/README.md)
- [Hermes Agent 무인 자동화 가이드](../../../HERMES_AUTOMATION_GUIDE.md)
- [이 작업 로그](./2026-08-14T013554.607907Z-clamshell-darkwake-guard-20260814-clamshell-darkwake-guard.md)

## 실행한 검증과 결과

- 새 테스트 RED: production API 부재로 2개 테스트가 실패하는 것을 확인했다.
- 새 테스트 GREEN: closed-lid probe와 선행 skip 테스트 2개 통과.
- Automation unittest: 92개 통과.
- Source Registry validator: schema `1.0`, 활성 출처 13개 통과.
- Vault note validator: 17개 note, issue 0개.
- Python compile: 통과.
- Markdown local link 검사: broken link 0개.
- `git diff --check`: 통과.
- 실제 macOS ioreg probe: `macos_clamshell_is_closed=True` 확인.
- active automation run: 해제 확인.
- root coordinator: clean `main` 확인.

## 내린 결정과 근거

- timeout 숫자를 늘리지 않았다. Mac이 잠든 동안 Python heartbeat와 tool timeout thread도 실행되지 않으므로 timeout 증가는 실패 시각만 늦춘다.
- `caffeinate`나 system sleep 정책을 자동 변경하지 않았다. clamshell sleep을 우회하는 전역 전원 변경은 사용자 시스템 전체에 영향을 주며 명시적 선택이 필요하다.
- closed clamshell에서는 정상 skip하는 fail-safe 기본값을 선택했다. 실패 이력과 stale lock은 막지만, 덮개가 다시 열릴 때까지 뉴스 처리가 지연된다는 한계는 문서에 명시했다.
- guard를 quota·Git·source 작업보다 앞에 배치했다. maintenance DarkWake의 약 45초 안에 결정론적으로 끝내기 위해서다.
- live cron은 merge되지 않은 controller를 사용하지 않도록 PR merge 전까지 paused 상태를 유지한다.

## 전역 설정이나 외부 시스템에 적용한 변경

- Hermes cron job `b83df32a868f`를 일시 정지했다.
- 마지막 stale active run을 controller를 통해 `irrelevant` 완료했다.
- credential, recipient, repository authorization, macOS power setting은 변경하지 않았다.
- Telegram 전송과 자동 merge는 수행하지 않았다.

## 알려진 한계와 남은 작업

- 닫힌 clamshell에서는 해당 cycle이 처리되지 않고 다음 fully-awake cycle까지 지연된다. 정확한 시각의 상시 처리가 필요하면 항상 켜진 Linux server 또는 Mac mini가 적합하다.
- 실제 closed-clamshell scheduled execution은 다음 물리적 closed-lid cycle에서 `clamshell_closed` 결과를 확인해야 한다. unit regression과 실제 open-lid controller load는 검증했다.
- 기존 실패 execution row는 감사 이력이라 삭제하지 않는다.

## 2026-08-18 운영 반영 및 검증

- PR #22가 merge commit `5e52644d1c5ba885baaa27ff1676964e0f81b931`로 merge됐고 Ubuntu·macOS·Windows CI가 모두 성공했음을 확인했다.
- root coordinator를 full fetch 후 `git merge --ff-only origin/main`으로 동기화하고 `HEAD == origin/main`, ahead/behind `0/0`, clean `main`을 확인했다.
- installed wrapper가 mode `0700`이고 workspace sidecar가 mode `0600`으로 root workspace를 가리키며, merged controller에서 clamshell guard가 로드됨을 확인했다.
- 첫 manual execution `d265f4b1e331478d9637b83c1d4cab92`은 foreground 확인 명령에 120초 제한을 잘못 적용해 실행 owner를 종료시켰다. controller run `20260818T044612482104Z-ee50a505` 자체는 Amex IR 컨퍼런스 일정 후보를 `irrelevant`로 완료했고 active lock을 해제했다.
- Gateway를 정상 재시작해 위 orphan execution을 공식 recovery 경로로 `unknown` 처리했다. DB row는 삭제하거나 성공·실패로 임의 변조하지 않았다.
- orphan execution의 clean worktree를 `git worktree remove`로 제거했다.
- cron을 다시 resume하고 `hermes cron run`을 추적 가능한 background process로 실행해 foreground timeout이 개입하지 않도록 했다.
- execution `b4a7c1080ca54dfcb3ff655b831ee80e`는 2026-08-18 15:01:12–15:03:25 KST에 `completed`로 끝났다.
- controller run `20260818T060113624648Z-a6656a95`는 PCI SSC AI Exchange 홍보 후보 1건을 `irrelevant`로 처리했으며 source failure 0, active lock 해제, worktree 제거, open PR 없음으로 종료됐다.
- Gateway PID `76383`, ticker heartbeat, cron active 상태와 다음 실행 `2026-08-18T18:03:25.103005+09:00`을 확인했다.
- 뉴스 note, commit, PR, Telegram은 관련 항목이 없어 생성하지 않았다.
