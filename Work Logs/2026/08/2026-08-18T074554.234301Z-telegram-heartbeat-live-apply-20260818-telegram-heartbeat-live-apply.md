# Telegram heartbeat merge 및 운영 적용

- 작업 일시: 2026-08-18 (UTC)
- task ID: `telegram-heartbeat-live-apply-20260818`
- 기준일: 2026-08-18
- 상태: merge·local sync·운영 source/prompt 적용 검증 완료, 실제 Telegram live smoke 미수행

## 사용자 요청과 목적

사용자가 [PR #24](https://github.com/dumbbelloper/hermes-agent/pull/24)를 merge했다고 알려 merge 결과와 CI를 검증하고, local coordinator `main` 및 실제 cron이 사용하는 skill runtime·prompt를 merged source와 동기화한다.

## 수행한 변경

- PR #24가 `MERGED`인지 확인했다.
  - merge 시각: 2026-08-18T07:42:59Z
  - merge commit: `6ca8781aacf6720f8f38928dc166e1d3a3b6b6da`
  - Ubuntu, macOS, Windows validation 모두 성공
- root coordinator에서 full origin refs를 fetch하고 `git merge --ff-only origin/main`을 실행했다.
- default Hermes profile에 설치된 `hermes-news-automation`의 다음 파일을 merged source와 동기화했다.
  - [SKILL.md](../../../skills/hermes-news-automation/SKILL.md)
  - [precheck.py](../../../skills/hermes-news-automation/scripts/precheck.py)
  - [automation.py](../../../skills/hermes-news-automation/scripts/runtime/hermes_agent/automation.py)
  - [cli.py](../../../skills/hermes-news-automation/scripts/runtime/hermes_agent/cli.py)
- cron job `b83df32a868f`의 live prompt를 [AUTONOMOUS_CRON_PROMPT.md](../../../Automation/AUTONOMOUS_CRON_PROMPT.md)와 동기화했다.
- 기존 schedule, attached skills, script, workdir, active 상태는 변경하지 않았다.

## 생성·수정한 문서와 파일

- 생성: 현재 작업 로그
- repository source file은 수정하지 않았다.
- 운영 시스템에서 default profile의 installed skill 파일 4개와 cron prompt metadata를 갱신했다.

## 실행한 검증과 결과

- GitHub PR #24: `MERGED`.
- CI: `validate (ubuntu-latest)`, `validate (macos-latest)`, `validate (windows-latest)` 모두 `SUCCESS`.
- local Git:
  - `HEAD == origin/main == 6ca8781aacf6720f8f38928dc166e1d3a3b6b6da`
  - ahead/behind `0/0`
  - root clean `main`
- installed skill 4개 파일: repository source와 byte-for-byte 일치.
- installed Python 3개 파일: `py_compile` 통과.
- live cron prompt: repository prompt와 trailing newline까지 일치.
- cron `b83df32a868f`: active, 180분 주기 유지.
- 권한:
  - installed `SKILL.md`: `0600`
  - installed Python 파일: `0644`
  - cron `jobs.json`: `0600`

## 내린 결정과 근거

- merge 확인만으로 기능이 운영 적용됐다고 판단하지 않았다. installed skill과 live cron prompt가 merged source와 실제로 불일치했기 때문이다.
- cron은 pause/resume하거나 Gateway를 restart하지 않았다. scheduler metadata는 `hermes cron edit`로 갱신됐고, 다음 agent session은 skill을 디스크에서 새로 로드한다.
- 배포 전 기존 운영 파일을 owner-only 임시 디렉터리에 backup하고, 적용 실패 시 복구하도록 했다. 적용과 검증 성공 후 backup을 제거했다.
- 실제 Telegram live smoke는 외부 메시지 전송 승인 없이 실행하지 않았다.

## 전역 설정이나 외부 시스템에 적용한 변경

- default Hermes profile의 installed `hermes-news-automation` bundle을 merged source로 갱신했다.
- cron job `b83df32a868f`의 prompt를 merged repository prompt로 갱신했다.
- GitHub `main` merge, branch 삭제, release, deploy, force push는 수행하지 않았다.

## 알려진 한계와 남은 작업

- 실제 Telegram heartbeat 도착은 아직 live 검증하지 않았다.
- 다음 정상 no-document cron cycle부터 merged heartbeat 경로가 사용된다.
- clamshell/disabled/quota early skip과 scheduler process timeout/crash 알림은 heartbeat 기능 범위가 아니다.
- 현재 문서 PR은 사용자가 검토·merge해야 하며 자동 merge하지 않는다.
